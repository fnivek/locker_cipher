"""Cipher implementations.

Each cipher is a pure function ``(int) -> int`` that maps an integer input to
an integer output.  The integer input is typically a locker number or a sum of
locker numbers.
"""
